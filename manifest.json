{
  "manifest_version": "0.2",
  "name": "frontier",
  "display_name": "Frontier",
  "version": "0.1.0",
  "description": "Builds verified historical timelines of technical fields and tracks their research frontier.",
  "long_description": "Frontier verifies historical milestone claims against primary sources and retrieves current research, so a timeline can be built from records rather than from recollection.\n\nEach claim field \u2014 entity, event type, date, superlative \u2014 is verified independently against free, keyless public APIs (arXiv, PubMed, Crossref, openFDA, Wikipedia, PatentsView). The server returns structured evidence: which sources attest to which values, how independent those sources are, and where the attested values split into distinct modes that suggest two real events have been merged under one label.\n\nThe server makes no model calls of its own. It retrieves, normalizes, deduplicates, caches, and scores; interpretation and naming are left to the model calling it.",
  "author": {
    "name": "WildCat231",
    "url": "https://github.com/WildCat231"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/WildCat231/MCP"
  },
  "license": "MIT",
  "keywords": [
    "research",
    "timeline",
    "verification",
    "citations",
    "literature",
    "provenance"
  ],
  "server": {
    "type": "node",
    "entry_point": "dist/index.js",
    "mcp_config": {
      "command": "node",
      "args": [
        "${__dirname}/dist/index.js"
      ],
      "env": {
        "FRONTIER_HOME": "${user_config.home}"
      }
    }
  },
  "tools": [
    {
      "name": "ping",
      "description": "Liveness check. Returns the frontier server name, version, and current time. Makes no network calls and touches no storage."
    },
    {
      "name": "search_literature",
      "description": "Search arXiv, PubMed, and Crossref for recent papers. Terms are ANDed to anchor field context; the date window adapts (6 to 60 months) when not supplied and is always reported as window_used."
    },
    {
      "name": "cache_status",
      "description": "Diagnostics for the on-disk cache: entry counts by namespace, freshness, age distribution, and hit rate by tool. Makes no network calls."
    },
    {
      "name": "clear_cache",
      "description": "Delete cached entries, optionally limited to one namespace."
    }
  ],
  "tools_generated": false,
  "user_config": {
    "home": {
      "type": "directory",
      "title": "Data directory",
      "description": "Where frontier keeps its cache and snapshots. Defaults to .frontier in your home directory.",
      "required": false
    }
  },
  "compatibility": {
    "platforms": [
      "darwin",
      "win32",
      "linux"
    ],
    "runtimes": {
      "node": ">=18.0.0"
    }
  }
}
